const axios = require('axios');

const getRecipesByName = async () => {
    try {
        
    } catch (error) {
        throw new Error("Ups! We got a problem.")  
    }
};

module.exports = {
	getRecipesByName,
};
